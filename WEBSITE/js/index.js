lottie.loadAnimation({
    container: twitter, // the dom element that will contain the animation
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'twitter.json' // the path to the animation json
  });